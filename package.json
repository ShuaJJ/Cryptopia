{
  "name": "@ShuaJJ/Cryptopia",
  "description": "Social platform for web3",
  "version": "1.0.0",
  "author": {
    "name": "ShuaJJ",
    "url": "https://github.com/ShuaJJ"
  },
  "devDependencies": {
    "@types/node": "^20.8.4",
    "prettier": "^3.0.0"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "forge",
    "foundry",
    "smart-contracts",
    "solidity",
    "template"
  ],
  "private": true,
  "scripts": {
    "clean": "rm -rf cache out",
    "build": "forge build",
    "lint": "pnpm lint:sol && pnpm prettier:check",
    "lint:sol": "forge fmt --check && pnpm solhint {script,src,test}/**/*.sol",
    "prettier:check": "prettier --check **/*.{json,md,yml} --ignore-path=.prettierignore",
    "prettier:write": "prettier --write **/*.{json,md,yml} --ignore-path=.prettierignore",
    "test": "forge test",
    "test:coverage": "forge coverage",
    "test:coverage:report": "forge coverage --report lcov && genhtml lcov.info --branch-coverage --output-dir coverage",
    "deploy:user:local": "forge script script/DeployUser.s.sol --broadcast --fork-url http://localhost:8545",
    "deploy:user:sepolia": "forge script script/DeployUser.s.sol --rpc-url https://sepolia.infura.io/v3/$API_KEY_INFURA --broadcast --verify --etherscan-api-key $API_KEY_ETHERSCAN",
    "verify:user:sepolia": "forge script script/VerifyUser.s.sol:VerifyUser 0x8E84d002c30Bf522233Fa1517ee2718D670fd578 $ADDRESS --sig 'run(address,address)' --fork-url https://sepolia.infura.io/v3/$API_KEY_INFURA --broadcast -vvv",
    "generate": "ts-node ./script/generateContractAddresses.ts"
  },
  "dependencies": {
    "solhint": "^3.6.2"
  }
}
